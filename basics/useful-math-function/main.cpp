#include <iostream>
#include<cmath>

// useful math function in cpp


int main () {

    double x = 3.99;
    double y = 3;
    double z;

    z = std::max(x, y);
//     // z = std::min(x, y);

//     // z = pow(2, 3);
//     // z = sqrt(81);
//     // z = abs(-24);
//     // z = round(x);
//     // z = ceil(x);
    // z = floor(x);

    std::cout << z << std::endl;

    return 0;
}